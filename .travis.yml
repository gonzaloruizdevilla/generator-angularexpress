language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angularexpress' ]; then cd .. && eval "mv $currentfolder generator-angularexpress" && cd generator-angularexpress; fi
